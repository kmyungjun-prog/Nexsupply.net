# API base URL (e.g. http://localhost:3000 when backend runs on 3000)
VITE_API_URL=http://localhost:3000

# Auth: set token in UI (Settings) or via sessionStorage/localStorage key "internal_review_token"
# Token must be Firebase ID token with role admin or system for /internal/* routes.
